const Logo = ({ className }: { className?: string }) => {
    return (
        <>
            <span className={`text-xl font-semibold ${className}`}>GEO Addons</span>
        </>
    )
}

export default Logo